rules_version = '2';

// Uploads happen via the Admin SDK (server-side); images are served through
// tokenized download URLs that work regardless of these rules. Deny all direct
// client access to Storage.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
